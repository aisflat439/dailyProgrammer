'use strict';

function findTwoSumParts(numberSet, target) {
  let targetSum = target,
      firstNumber = 0,
      secondNumber = 0;

  numberSet.forEach((item, index, arr) => {
    for (let i = 0; i < arr.length; i++) {
      
    }
  });

  return 'derp';
}

console.log('should return 13');
console.log(findTwoSumParts([2, 5, 76, 8], 13));
