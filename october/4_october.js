function circularShift(shiftArray, shiftValue) {
  let newarry = [];
  shiftArray.forEach((item, index, array) => {
    for (let i = 0; i < shiftValue; i++) {
      console.log();
    }
  });
  return newarry;
}

console.log(circularShift([2, 4, 6, -2], 3));
